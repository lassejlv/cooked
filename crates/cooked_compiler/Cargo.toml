[package]
name = "cooked_compiler"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
oxc = { version = "0.40", features = ["semantic"] }

[dev-dependencies]
insta = "1"
