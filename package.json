{
  "name": "cooked-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Cooked — a Svelte/Solid-style compiled UI language. Rust x TypeScript.",
  "type": "module",
  "scripts": {
    "build:compiler": "pnpm --filter @cooked/binding build",
    "build:plugin": "pnpm --filter vite-plugin-cooked build",
    "build:runtime": "pnpm --filter cooked build",
    "build": "pnpm build:runtime && pnpm build:compiler && pnpm build:plugin",
    "check": "cargo fmt --check && cargo check && cargo test -p cooked_compiler && pnpm build && pnpm test",
    "test": "pnpm -r test",
    "example": "pnpm --filter @cooked/example-counter dev"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@11.5.2"
}
